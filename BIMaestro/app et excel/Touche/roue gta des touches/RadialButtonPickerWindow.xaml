<Window x:Class="BIMaestro.UI.RadialButtonPickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Choisir un bouton" Height="500" Width="430" MinHeight="360" MinWidth="360"
        WindowStartupLocation="CenterOwner" Background="{DynamicResource App.Background}">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/BIMaestro;component/Themes/BIMaestroTheme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBox x:Name="SearchBox" Height="34" Margin="0,0,0,10"
                 ToolTip="Rechercher un bouton" TextChanged="SearchChanged"/>
        <ListBox x:Name="ButtonsList" Grid.Row="1" MouseDoubleClick="ButtonsList_DoubleClick">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Margin="4">
                        <Image Source="{Binding ImagePath}" Width="30" Height="30" Margin="0,0,10,0"/>
                        <TextBlock Text="{Binding Label}" VerticalAlignment="Center" TextWrapping="Wrap"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Style="{StaticResource SecondaryButton}" Content="Annuler" Margin="0,0,8,0" Click="Cancel_Click"/>
            <Button Style="{StaticResource PrimaryButton}" Content="Choisir" Click="Choose_Click"/>
        </StackPanel>
    </Grid>
</Window>
