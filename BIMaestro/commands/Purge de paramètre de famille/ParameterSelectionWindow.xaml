 <Window x:Class="Famille.ParameterSelectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="BIMaestro - Sélectionner les paramètres à supprimer"
        WindowStartupLocation="CenterScreen"
        Height="620" Width="640"
        MinHeight="420" MinWidth="520"
        Background="{DynamicResource App.Background}"
        ResizeMode="CanResizeWithGrip">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/BIMaestro;component/Themes/BIMaestroTheme.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <CollectionViewSource x:Key="GroupedParameters" Source="{Binding Parameters}">
                <CollectionViewSource.GroupDescriptions>
                    <PropertyGroupDescription PropertyName="Group"/>
                </CollectionViewSource.GroupDescriptions>
            </CollectionViewSource>

            <Style TargetType="DataGrid">
                <Setter Property="RowHeight" Value="30"/>
                <Setter Property="ColumnHeaderHeight" Value="34"/>
                <Setter Property="FontFamily" Value="Segoe UI"/>
                <Setter Property="FontSize" Value="12"/>
                <Setter Property="Background" Value="{DynamicResource Surface}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource Border}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="GridLinesVisibility" Value="Horizontal"/>
                <Setter Property="HorizontalGridLinesBrush" Value="{DynamicResource Divider}"/>
                <Setter Property="VerticalGridLinesBrush" Value="{DynamicResource Divider}"/>
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Background="{DynamicResource Brand}"
                CornerRadius="14"
                Padding="16"
                Margin="0,0,0,12">
            <StackPanel>
                <TextBlock Style="{StaticResource H1}" Text="Sélection des paramètres"/>
                <TextBlock Style="{StaticResource Hint}"
                           Foreground="White"
                           Margin="0,6,0,0"
                           Text="Sélectionnez les paramètres à supprimer, puis validez pour lancer le nettoyage."/>
            </StackPanel>
        </Border>

        <Border Grid.Row="1" Style="{StaticResource Card}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0"
                           Style="{StaticResource H2}"
                           Text="Paramètres disponibles"/>

                <DataGrid Grid.Row="1"
                          x:Name="dataGridParameters"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          Margin="0,0,0,4"
                          ItemsSource="{Binding Source={StaticResource GroupedParameters}}"
                          VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Auto">
                    <DataGrid.GroupStyle>
                        <GroupStyle>
                            <GroupStyle.HeaderTemplate>
                                <DataTemplate>
                                    <Border Background="{DynamicResource Surface.Subtle}"
                                            BorderBrush="{DynamicResource Border}"
                                            BorderThickness="1"
                                            CornerRadius="8"
                                            Padding="8,4"
                                            Margin="0,6,0,4">
                                        <TextBlock Text="{Binding Name}" FontWeight="SemiBold"/>
                                    </Border>
                                </DataTemplate>
                            </GroupStyle.HeaderTemplate>
                        </GroupStyle>
                    </DataGrid.GroupStyle>
                    <DataGrid.Columns>
                        <DataGridCheckBoxColumn Header="Supprimer" Binding="{Binding IsSelected}" Width="Auto"/>
                        <DataGridTextColumn Header="Nom du paramètre" Binding="{Binding Name}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button x:Name="ToggleButton" Style="{StaticResource SecondaryButton}" Content="Tout décocher" MinWidth="140" Margin="0,0,8,0" Click="ToggleSelection_Click"/>
            <Button Style="{StaticResource PrimaryButton}" Content="OK" MinWidth="100" Margin="0,0,8,0" Click="OK_Click"/>
            <Button Style="{StaticResource SecondaryButton}" Content="Annuler" MinWidth="100" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>