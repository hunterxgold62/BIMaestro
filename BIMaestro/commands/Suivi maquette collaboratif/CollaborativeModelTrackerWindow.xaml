<Window x:Class="Analyse.CollaborativeModelTrackerWindow"
          xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Suivi maquettes collaboratif"
        Height="650"
        Width="1280"
        MinWidth="1220"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        Background="{DynamicResource App.Background}"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/BIMaestro;component/Themes/BIMaestroTheme.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <Style x:Key="FilterLabel" TargetType="TextBlock" BasedOn="{StaticResource Label}">
                <Setter Property="Foreground" Value="{DynamicResource Text.Secondary}"/>
                <Setter Property="Margin" Value="0,0,8,0"/>
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                CornerRadius="16"
                Background="{StaticResource Brand}"
                Padding="18"
                Margin="0,0,0,14">
            <StackPanel>
                <TextBlock Text="Suivi des maquettes collaboratives" Style="{StaticResource H1}"/>
                <TextBlock Margin="0,6,0,0"
                           Text="Filtrez rapidement par utilisateur, version Revit et type de fichier pour ouvrir le bon dossier de travail."
                           Foreground="#E7F6EE"
                           TextWrapping="Wrap"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="1" Style="{StaticResource Card}" Margin="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="190"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="170"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="170"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="220"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" VerticalAlignment="Center" Text="Utilisateur :" Style="{StaticResource FilterLabel}"/>
                <ComboBox x:Name="UserComboBox"
                          Grid.Column="1"
                          IsEditable="True"                          
                          IsTextSearchEnabled="True"
                          Margin="0,0,12,0"
                          SelectionChanged="UserComboBox_SelectionChanged"
                          LostFocus="UserComboBox_LostFocus"/>

                <TextBlock Grid.Column="2" VerticalAlignment="Center" Text="Version Revit :" Style="{StaticResource FilterLabel}"/>
                <ComboBox x:Name="RevitVersionComboBox"
                          Grid.Column="3"
                          IsEditable="False"
                          Margin="0,0,12,0"
                          SelectionChanged="RevitVersionComboBox_SelectionChanged"/>

                <TextBlock Grid.Column="4" VerticalAlignment="Center" Text="Type fichier :" Style="{StaticResource FilterLabel}"/>
                <ComboBox x:Name="FileTypeComboBox"
                          Grid.Column="5"
                          IsEditable="False"
                          Margin="0,0,12,0"
                          SelectionChanged="FileTypeComboBox_SelectionChanged"/>

                <TextBlock Grid.Column="6" VerticalAlignment="Center" Text="Recherche maquette :" Style="{StaticResource FilterLabel}"/>
                <TextBox x:Name="SearchModelTextBox"
                         Grid.Column="7"
                         Margin="0,0,12,0"
                         VerticalContentAlignment="Center"
                         TextChanged="SearchModelTextBox_TextChanged"/>

            </Grid>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource Card}" Padding="10">
            <ListBox x:Name="ProjectsListBox"
                     Background="Transparent"
                     BorderThickness="0"
                     MouseDoubleClick="ProjectsListBox_MouseDoubleClick"
                     HorizontalContentAlignment="Stretch"
                     ScrollViewer.CanContentScroll="True"
                     ScrollViewer.IsDeferredScrollingEnabled="True"
                     VirtualizingPanel.IsVirtualizing="True"
                     VirtualizingPanel.VirtualizationMode="Recycling"
                     SnapsToDevicePixels="True">
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <VirtualizingStackPanel IsVirtualizing="True"
                                                VirtualizationMode="Recycling"/>
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="Padding" Value="0"/>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{Binding PastelBrush}"
                                HorizontalAlignment="Stretch"
                                CornerRadius="12"
                                Padding="12"
                                Margin="0,0,0,8"
                                BorderBrush="#66FFFFFF"
                                BorderThickness="1">
                            <StackPanel>
                                <TextBlock Text="{Binding ModelName}" FontSize="17" FontWeight="Bold" Foreground="{DynamicResource Text.Primary}"/>
                                <TextBlock Text="{Binding ProjectName}" Margin="0,4,0,0" FontWeight="SemiBold" Opacity="0.9" Foreground="{DynamicResource Text.Primary}"/>
                                <TextBlock Text="{Binding ModelPath}" Margin="0,3,0,0" TextWrapping="Wrap" Opacity="0.85" Foreground="{DynamicResource Text.Secondary}"/>
                                <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                                    <TextBlock Text="Utilisateur : " FontWeight="SemiBold" Foreground="{DynamicResource Text.Primary}"/>
                                    <TextBlock Text="{Binding UserName}" Foreground="{DynamicResource Text.Primary}"/>
                                    <TextBlock Text="   Créateur : " FontWeight="SemiBold" Margin="12,0,0,0" Foreground="{DynamicResource Text.Primary}"/>
                                    <TextBlock Text="{Binding CreatorName}" Foreground="{DynamicResource Text.Primary}"/>
                                    <TextBlock Text="   Revit : " FontWeight="SemiBold" Margin="12,0,0,0" Foreground="{DynamicResource Text.Primary}"/>
                                    <TextBlock Text="{Binding RevitVersion}" Foreground="{DynamicResource Text.Primary}"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>

        <Grid Grid.Row="3" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock x:Name="InfoText"
                       Grid.Column="0"
                       Opacity="0.9"
                       Foreground="{DynamicResource Text.Secondary}"
                       TextWrapping="Wrap"
                       VerticalAlignment="Center"/>

            <Button Grid.Column="1"
                    Content="Ouvrir dossier"
                    Width="150"
                    Style="{StaticResource PrimaryButton}"
                    HorizontalAlignment="Right"
                    Click="OpenFolder_Click"/>
        </Grid>
    </Grid>
</Window>