<Window x:Class="Analyse.ProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="BIMaestro - Analyse en cours"
        Height="220"
        Width="560"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="{DynamicResource App.Background}"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/BIMaestro;component/Themes/BIMaestroTheme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="16">
        <Border Style="{StaticResource Card}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Analyse en cours" Style="{StaticResource H2}" Margin="0,0,0,6"/>

                <TextBlock x:Name="StatusText"
                           Grid.Row="1"
                           FontSize="14"
                           Foreground="{DynamicResource Text.Primary}"
                           Margin="0,0,0,10"
                           TextWrapping="Wrap"/>

                <Grid Grid.Row="2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <ProgressBar x:Name="ProgressBar"
                                 Height="24"
                                 Minimum="0"
                                 Maximum="100"
                                 VerticalAlignment="Center"
                                 Margin="0,0,12,0"/>

                    <Button Grid.Column="1"
                            Content="Annuler"
                            Width="120"
                            Style="{StaticResource SecondaryButton}"
                            Click="CancelButton_Click"/>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</Window>