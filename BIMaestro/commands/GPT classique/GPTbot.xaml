<Window x:Class="IA.GPTBotWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:IA"
        Title="BIMaestro - GPT Bot"
        Height="560"
        Width="620"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="{DynamicResource App.Background}">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/BIMaestro;component/Themes/BIMaestroTheme.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <DataTemplate x:Key="UserMessageTemplate">
                <StackPanel HorizontalAlignment="Right" Margin="0,8,0,0">
                    <Border Style="{DynamicResource Card}"
                            Background="{DynamicResource Brand}"
                            BorderBrush="{DynamicResource Brand}"
                            MaxWidth="420"
                            Margin="50,0,0,0"
                            Padding="12">
                        <TextBox Text="{Binding Content}"
                                 TextWrapping="Wrap"
                                 Foreground="White"
                                 Background="Transparent"
                                 BorderThickness="0"
                                 IsReadOnly="True"
                                 IsReadOnlyCaretVisible="True"
                                 Cursor="IBeam"/>
                    </Border>
                </StackPanel>
            </DataTemplate>

            <DataTemplate x:Key="BotMessageTemplate">
                <StackPanel HorizontalAlignment="Left" Margin="0,8,0,0">
                    <Border Style="{DynamicResource Card}"
                            MaxWidth="420"
                            Margin="0,0,50,0"
                            Padding="12">
                        <TextBox Text="{Binding Content}"
                                 TextWrapping="Wrap"
                                 Foreground="{DynamicResource Text.Primary}"
                                 Background="Transparent"
                                 BorderThickness="0"
                                 IsReadOnly="True"
                                 IsReadOnlyCaretVisible="True"
                                 Cursor="IBeam"/>
                    </Border>
                </StackPanel>
            </DataTemplate>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{DynamicResource Brand}" Padding="16,12" CornerRadius="0,0,12,12">
            <TextBlock Text="Assistant BIMaestro" Style="{DynamicResource H1}"/>
        </Border>

        <Border Grid.Row="1" Style="{DynamicResource Card}" Margin="16" Padding="12">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <ScrollViewer Grid.Row="0"
                              VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled"
                              PreviewMouseWheel="ScrollViewer_PreviewMouseWheel">
                    <ListBox x:Name="MessagesListBox"
                             ItemsSource="{Binding}"
                             Background="Transparent"
                             BorderThickness="0"
                             Foreground="{DynamicResource Text.Primary}">
                        <ListBox.Resources>
                            <DataTemplate DataType="{x:Type local:MessageModel}">
                                <ContentControl Content="{Binding}">
                                    <ContentControl.Style>
                                        <Style TargetType="ContentControl">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Role}" Value="system">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Role}" Value="user">
                                                    <Setter Property="ContentTemplate" Value="{StaticResource UserMessageTemplate}"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Role}" Value="assistant">
                                                    <Setter Property="ContentTemplate" Value="{StaticResource BotMessageTemplate}"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </ContentControl.Style>
                                </ContentControl>
                            </DataTemplate>
                        </ListBox.Resources>
                    </ListBox>
                </ScrollViewer>

                <ProgressBar x:Name="LoadingIndicator"
                             Grid.Row="1"
                             IsIndeterminate="True"
                             Visibility="Collapsed"
                             Height="8"
                             Margin="4,10,4,2"
                             Foreground="{DynamicResource Brand}"
                             Background="{DynamicResource Divider}"/>
            </Grid>
        </Border>

        <Border Grid.Row="2" Style="{DynamicResource Card}" Margin="16,0,16,16" Padding="12">
            <DockPanel LastChildFill="True">
                <TextBox x:Name="InputBox"
                         DockPanel.Dock="Left"
                         Height="36"
                         VerticalAlignment="Center"
                         Margin="0,0,10,0"
                         Padding="10,2"
                         MinWidth="300"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="ElementButton"
                            Content="Élément"
                            Width="120"
                            Margin="0,0,8,0"
                            Style="{DynamicResource SecondaryButton}"
                            Click="ElementButton_Click"
                            IsEnabled="True"/>
                    <Button x:Name="AskButton"
                            Content="Envoyer"
                            Width="120"
                            Style="{DynamicResource PrimaryButton}"
                            Click="AskButton_Click"
                            IsEnabled="True"/>
                </StackPanel>
            </DockPanel>
        </Border>
    </Grid>
</Window>