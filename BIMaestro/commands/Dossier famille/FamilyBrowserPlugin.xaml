<Window x:Class="Famille.FamilyBrowserWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Famille"
        xmlns:ui="clr-namespace:BIMaestro.UI"
        Title="BIMaestro - Navigateur de Familles"
        Height="651" Width="1280"
        MinWidth="900" MinHeight="400"
        FontFamily="Segoe UI" FontSize="12"
        Background="{DynamicResource App.Background}"
        Foreground="{DynamicResource Text.Primary}"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        Loaded="Window_Loaded">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/BIMaestro;component/Themes/BIMaestroTheme.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <!-- Thème -->
            <SolidColorBrush x:Key="PrimaryText" Color="#0F172A"/>
            <SolidColorBrush x:Key="TabInactiveForeground" Color="#64748B"/>
            <SolidColorBrush x:Key="TabActiveForeground" Color="#0F172A"/>
            <SolidColorBrush x:Key="SearchBoxBackground" Color="#FFFFFF"/>
            <SolidColorBrush x:Key="SearchBoxForeground" Color="#0F172A"/>
            <SolidColorBrush x:Key="SearchBoxBorder" Color="#E6EAF0"/>
            <LinearGradientBrush x:Key="BackgroundGradient" StartPoint="0,0" EndPoint="0,1">
                <GradientStop Offset="0" Color="#F6F8FB"/>
                <GradientStop Offset="1" Color="#FFFFFF"/>
            </LinearGradientBrush>
            <SolidColorBrush x:Key="PanelBackground"    Color="#F9FAFB"/>
            <SolidColorBrush x:Key="ContentPanelBackground" Color="#F0F0F0"/>
            <SolidColorBrush x:Key="TreeViewBackground" Color="#F9FAFB"/>
            <SolidColorBrush x:Key="ItemsBackground"    Color="Transparent"/>
            <SolidColorBrush x:Key="TabBackground"      Color="Transparent"/>
            <SolidColorBrush x:Key="ImageBackground"    Color="Transparent"/>

            <ItemsPanelTemplate x:Key="FamilyListWrapPanel">
                <WrapPanel IsItemsHost="True"/>
            </ItemsPanelTemplate>

            <ItemsPanelTemplate x:Key="FamilyListStackPanel">
                <StackPanel IsItemsHost="True" Orientation="Vertical"/>
            </ItemsPanelTemplate>

            <!-- Styles -->
            <Style TargetType="TreeViewItem">
                <Setter Property="Foreground" Value="{DynamicResource PrimaryText}"/>
            </Style>

            <Style x:Key="ImageStyle" TargetType="Image">
                <Setter Property="Effect">
                    <Setter.Value>
                        <DropShadowEffect ShadowDepth="2" BlurRadius="8" Opacity="0.28"/>
                    </Setter.Value>
                </Setter>
            </Style>
            <Style x:Key="PreviewFrameStyle" TargetType="Border">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="BorderBrush" Value="Transparent"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="CornerRadius" Value="8"/>
                <Setter Property="Padding" Value="0"/>
                <Setter Property="ClipToBounds" Value="True"/>
            </Style>
            <Style x:Key="PillButtonStyle" TargetType="Button">
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Background" Value="{DynamicResource Surface.Subtle}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource Border}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Padding" Value="10,4"/>
                <Setter Property="Margin" Value="0,0,8,8"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Foreground" Value="{DynamicResource Text.Primary}"/>
                <Setter Property="Focusable" Value="False"/>
                <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="12"
                                Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="BreadcrumbButtonStyle" TargetType="Button">
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Padding" Value="4,2"/>
                <Setter Property="Foreground" Value="{DynamicResource Brand}"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            </Style>


            <Style x:Key="FavoritesToolbarCard" TargetType="Border" BasedOn="{StaticResource Card}">
                <Setter Property="Padding" Value="12"/>
                <Setter Property="CornerRadius" Value="10"/>
            </Style>

            <Style x:Key="FavoritesActionButton" TargetType="Button" BasedOn="{StaticResource SecondaryButton}">
                <Setter Property="Height" Value="34"/>
                <Setter Property="MinWidth" Value="74"/>
                <Setter Property="Padding" Value="12,0"/>
            </Style>

            <Style x:Key="FavoritesPrimaryActionButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
                <Setter Property="Height" Value="34"/>
                <Setter Property="MinWidth" Value="150"/>
                <Setter Property="Padding" Value="12,0"/>
            </Style>

            <Style x:Key="DocumentationBadgeStyle" TargetType="Button">
                <Setter Property="Content" Value="DOC"/>
                <Setter Property="FontSize" Value="10"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Padding" Value="8,4"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Background" Value="{DynamicResource Surface}"/>
                <Setter Property="Foreground" Value="{DynamicResource Brand}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="BorderBrush" Value="{DynamicResource Brand}"/>
                <Setter Property="Visibility" Value="Collapsed"/>
                <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                <Setter Property="ToolTip" Value="Ouvrir la documentation"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="12"
                                Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Style.Triggers>
                    <DataTrigger Binding="{Binding DocumentationAvailable}" Value="True">
                        <Setter Property="Visibility" Value="Visible"/>
                    </DataTrigger>
                </Style.Triggers>
            </Style>
            <Style x:Key="FreshnessBadgeStyle" TargetType="Border">
                <Setter Property="HorizontalAlignment" Value="Right"/>
                <Setter Property="VerticalAlignment" Value="Top"/>
                <Setter Property="CornerRadius" Value="9"/>
                <Setter Property="Padding" Value="7,2"/>
                <Setter Property="Margin" Value="0,34,8,0"/>
                <Setter Property="Background" Value="#F8FFFFFF"/>
                <Setter Property="BorderBrush" Value="#FFDCE6FA"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Visibility" Value="Collapsed"/>
            </Style>

            <!-- Vignette famille (onglet Dossiers) -->
            <DataTemplate x:Key="FamilyItemTemplate" DataType="{x:Type local:FamilyItem}">
                <Border Margin="6"
                    Width="{Binding DataContext.TileCardWidth, RelativeSource={RelativeSource AncestorType=Window}}"
                    Background="{DynamicResource PanelBackground}"
                    CornerRadius="5"
                    BorderThickness="1"
                    MouseLeftButtonDown="FamilyItem_DoubleClick"
                    PreviewMouseRightButtonDown="FamilyItem_RightClickSelect"
                    ContextMenuOpening="FamilyItem_ContextMenuOpening">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Setter Property="BorderBrush" Value="#33000000"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                    <Setter Property="BorderBrush" Value="{DynamicResource Brand}"/>
                                    <Setter Property="BorderThickness" Value="3"/>
                                    <Setter Property="Background" Value="#1F0F5132"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <Border.ContextMenu>
                        <ContextMenu>
                            <MenuItem Header="Ouvrir le dossier"
                                  Click="OpenFolderItem_Click"
                                  DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                            <Separator/>
                            <MenuItem Header="Documentation"
                                  Click="DocumentationMenuItem_Click"
                                  DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                            <MenuItem Header="Mots-clés de recherche…"
                                  Click="SearchMetadataMenuItem_Click"
                                  DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                            <Separator/>
                            <MenuItem Header="Rentrer dans la famille"
                                  Click="OpenFamilyFile_Click"
                                  DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                            <MenuItem Header="Charger la dernière version"
                                  Click="ReloadFamily_Click"
                                  DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>

                            <Separator/>

                            <!-- Ajout express dans la collection active -->
                            <MenuItem Header="Ajouter à la collection active"
                                  Click="AddToActiveCollection_Click"
                                  DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                        </ContextMenu>
                    </Border.ContextMenu>

                    <Grid Margin="6">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <Grid Grid.Row="0">
                            <Border Style="{StaticResource PreviewFrameStyle}"
                                    Visibility="{Binding FamilyControlVisibility}">
                                <Image Source="{Binding Icon}"
                                   Width="{Binding DataContext.TilePreviewSize, RelativeSource={RelativeSource AncestorType=Window}}"
                                   Height="{Binding DataContext.TilePreviewSize, RelativeSource={RelativeSource AncestorType=Window}}"
                                   Stretch="Uniform"
                                   RenderOptions.BitmapScalingMode="HighQuality"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   />
                            </Border>
                            <Border Style="{StaticResource PreviewFrameStyle}"
                                    Visibility="{Binding FolderControlVisibility}"
                                    ToolTip="Ouvrir le sous-dossier">
                                <Grid Width="{Binding DataContext.TilePreviewSize, RelativeSource={RelativeSource AncestorType=Window}}"
                                      Height="{Binding DataContext.TilePreviewSize, RelativeSource={RelativeSource AncestorType=Window}}">
                                    <Border CornerRadius="7" Background="Transparent"/>
                                    <Grid Margin="3" Visibility="{Binding FolderPreviewVisibility}">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="*"/>
                                            <RowDefinition Height="*"/>
                                        </Grid.RowDefinitions>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <Border Grid.Row="0" Grid.Column="0" Margin="2" CornerRadius="5" ClipToBounds="True" Background="Transparent">
                                            <Image Source="{Binding FolderPreviewIcon1}" Stretch="Uniform" RenderOptions.BitmapScalingMode="HighQuality"/>
                                        </Border>
                                        <Border Grid.Row="0" Grid.Column="1" Margin="2" CornerRadius="5" ClipToBounds="True" Background="Transparent">
                                            <Image Source="{Binding FolderPreviewIcon2}" Stretch="Uniform" RenderOptions.BitmapScalingMode="HighQuality"/>
                                        </Border>
                                        <Border Grid.Row="1" Grid.Column="0" Margin="2" CornerRadius="5" ClipToBounds="True" Background="Transparent">
                                            <Image Source="{Binding FolderPreviewIcon3}" Stretch="Uniform" RenderOptions.BitmapScalingMode="HighQuality"/>
                                        </Border>
                                        <Border Grid.Row="1" Grid.Column="1" Margin="2" CornerRadius="5" ClipToBounds="True" Background="Transparent">
                                            <Image Source="{Binding FolderPreviewIcon4}" Stretch="Uniform" RenderOptions.BitmapScalingMode="HighQuality"/>
                                        </Border>
                                    </Grid>
                                    <TextBlock Text="←"
                                               Visibility="{Binding BackNavigationVisibility}"
                                               FontSize="64"
                                               FontWeight="SemiBold"
                                               Foreground="{DynamicResource Brand}"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center"/>
                                    <TextBlock Text="↑"
                                               Visibility="{Binding ScrollTopNavigationVisibility}"
                                               FontSize="64"
                                               FontWeight="SemiBold"
                                               Foreground="{DynamicResource Brand}"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center"/>
                                    <Border HorizontalAlignment="Left"
                                            VerticalAlignment="Bottom"
                                            Margin="8"
                                            Padding="7,2"
                                            CornerRadius="10"
                                            Background="#F8FFFFFF"
                                            BorderBrush="#FFDCE6FA"
                                            BorderThickness="1">
                                        <TextBlock Text="{Binding FolderBadgeText}"
                                                   Foreground="{DynamicResource Brand}"
                                                   FontWeight="SemiBold"
                                                   FontSize="10"/>
                                    </Border>
                                </Grid>
                            </Border>
                            <Button Style="{StaticResource DocumentationBadgeStyle}"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Top"
                                Margin="6"
                                Click="DocumentationBadge_Click"/>
                            <Border>
                                <Border.Style>
                                    <Style TargetType="Border" BasedOn="{StaticResource FreshnessBadgeStyle}">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsNew}" Value="True">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <TextBlock Text="NEW" Foreground="{DynamicResource Brand}" FontWeight="SemiBold" FontSize="10"/>
                            </Border>
                            <Border>
                                <Border.Style>
                                    <Style TargetType="Border" BasedOn="{StaticResource FreshnessBadgeStyle}">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsUpdated}" Value="True">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <TextBlock Text="MAJ" Foreground="{DynamicResource Brand}" FontWeight="SemiBold" FontSize="10"/>
                            </Border>
                        </Grid>
                        <Button Content="3D"
                            Width="28" Height="20" FontSize="10" Padding="0"
                            HorizontalAlignment="Right" VerticalAlignment="Top" Margin="8"
                            Click="OnPreview3DClick"
                            ToolTip="Aperçu 3D de la famille"
                            Background="Transparent"
                            BorderThickness="0"
                            BorderBrush="Transparent"
                            FocusVisualStyle="{x:Null}"
                            Visibility="{Binding FamilyControlVisibility}"/>
                        <Grid Grid.Row="1">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- ★ : Favoris -->
                            <Button Grid.Column="0" Content="★"
                                Click="FavoriteButton_Click"
                                ToolTip="Ajouter/retirer des Favoris"
                                Background="Transparent"
                                BorderThickness="0"
                                FontSize="16"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Top"
                                Margin="0,0,5,0"
                                Visibility="{Binding FamilyControlVisibility}">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Foreground" Value="SkyBlue"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsFavorite}" Value="True">
                                                <Setter Property="Foreground" Value="Orange"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>

                            <TextBlock Grid.Column="1"
                                   Text="{Binding Name}"
                                   Foreground="{DynamicResource PrimaryText}"
                                   FontWeight="Bold"
                                   FontSize="{Binding DataContext.TileNameFontSize, RelativeSource={RelativeSource AncestorType=Window}}"
                                   TextWrapping="Wrap"
                                   TextTrimming="CharacterEllipsis"
                                   LineStackingStrategy="BlockLineHeight"
                                   LineHeight="{Binding DataContext.TileNameLineHeight, RelativeSource={RelativeSource AncestorType=Window}}"
                                   MaxHeight="{Binding DataContext.TileNameMaxHeight, RelativeSource={RelativeSource AncestorType=Window}}"
                                   MaxWidth="{Binding DataContext.TileNameMaxWidth, RelativeSource={RelativeSource AncestorType=Window}}"
                                   TextAlignment="Center"
                                   HorizontalAlignment="Center"
                                   Margin="0,4,5,0"
                                   ToolTip="{Binding Name}"/>
                        </Grid>
                        <StackPanel Grid.Row="2"
                                    Margin="26,3,8,0"
                                    Visibility="{Binding SearchDetailsVisibility}">
                            <TextBlock Text="{Binding ApproximateMatchText}"
                                       Visibility="{Binding ApproximateMatchVisibility}"
                                       Foreground="#FFD97706"
                                       FontWeight="SemiBold"
                                       FontSize="10"
                                       TextWrapping="Wrap"
                                       TextAlignment="Center"/>
                            <TextBlock Text="{Binding MatchedKeywordsText}"
                                       Visibility="{Binding MatchedKeywordsVisibility}"
                                       Foreground="#FF7C3AED"
                                       FontWeight="SemiBold"
                                       FontSize="10"
                                       TextWrapping="Wrap"
                                       TextAlignment="Center"
                                       ToolTip="{Binding SearchDescription}"/>
                            <TextBlock Text="{Binding SearchLocationLabel}"
                                       Visibility="{Binding SearchLocationVisibility}"
                                       Foreground="{DynamicResource Brand}"
                                       FontWeight="SemiBold"
                                       FontSize="10"
                                       TextAlignment="Center"/>
                            <TextBlock Text="{Binding SearchFolderPath}"
                                       Visibility="{Binding SearchLocationVisibility}"
                                       Foreground="#FF6C7A92"
                                       FontSize="10"
                                       TextWrapping="Wrap"
                                       TextTrimming="CharacterEllipsis"
                                       MaxHeight="28"
                                       TextAlignment="Center"
                                       ToolTip="{Binding SearchFolderPath}"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </DataTemplate>

            <DataTemplate x:Key="FamilyItemDetailedTemplate" DataType="{x:Type local:FamilyItem}">
                <Border Margin="5"
                    Background="{DynamicResource PanelBackground}"
                    CornerRadius="5"
                    BorderThickness="1"
                    MouseLeftButtonDown="FamilyItem_DoubleClick"
                    PreviewMouseRightButtonDown="FamilyItem_RightClickSelect"
                    ContextMenuOpening="FamilyItem_ContextMenuOpening"
                    HorizontalAlignment="Stretch">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Setter Property="BorderBrush" Value="#33000000"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                    <Setter Property="BorderBrush" Value="{DynamicResource Brand}"/>
                                    <Setter Property="BorderThickness" Value="3"/>
                                    <Setter Property="Background" Value="#1F0F5132"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <Border.ContextMenu>
                        <ContextMenu>
                            <MenuItem Header="Ouvrir le dossier"
                                  Click="OpenFolderItem_Click"
                                  DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                            <Separator/>
                            <MenuItem Header="Documentation"
                                  Click="DocumentationMenuItem_Click"
                                  DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                            <MenuItem Header="Mots-clés de recherche…"
                                  Click="SearchMetadataMenuItem_Click"
                                  DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                            <Separator/>
                            <MenuItem Header="Rentrer dans la famille"
                                  Click="OpenFamilyFile_Click"
                                  DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                            <MenuItem Header="Charger la dernière version"
                                  Click="ReloadFamily_Click"
                                  DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>

                            <Separator/>

                            <MenuItem Header="Ajouter à la collection active"
                                  Click="AddToActiveCollection_Click"
                                  DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                        </ContextMenu>
                    </Border.ContextMenu>

                    <Grid Margin="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="180"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Grid Grid.Column="0" HorizontalAlignment="Left">
                            <Border Style="{StaticResource PreviewFrameStyle}"
                                Visibility="{Binding FamilyControlVisibility}">
                                <Image Source="{Binding Icon}"
                                   Width="196" Height="196"
                                   Stretch="Uniform"
                                   RenderOptions.BitmapScalingMode="HighQuality"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   />
                            </Border>
                            <Border Style="{StaticResource PreviewFrameStyle}"
                                    Visibility="{Binding FolderControlVisibility}"
                                    ToolTip="Ouvrir le sous-dossier">
                                <Grid Width="196" Height="196">
                                    <Border CornerRadius="7" Background="Transparent"/>
                                    <Grid Margin="3" Visibility="{Binding FolderPreviewVisibility}">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="*"/>
                                            <RowDefinition Height="*"/>
                                        </Grid.RowDefinitions>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <Border Grid.Row="0" Grid.Column="0" Margin="2" CornerRadius="5" ClipToBounds="True" Background="Transparent">
                                            <Image Source="{Binding FolderPreviewIcon1}" Stretch="Uniform" RenderOptions.BitmapScalingMode="HighQuality"/>
                                        </Border>
                                        <Border Grid.Row="0" Grid.Column="1" Margin="2" CornerRadius="5" ClipToBounds="True" Background="Transparent">
                                            <Image Source="{Binding FolderPreviewIcon2}" Stretch="Uniform" RenderOptions.BitmapScalingMode="HighQuality"/>
                                        </Border>
                                        <Border Grid.Row="1" Grid.Column="0" Margin="2" CornerRadius="5" ClipToBounds="True" Background="Transparent">
                                            <Image Source="{Binding FolderPreviewIcon3}" Stretch="Uniform" RenderOptions.BitmapScalingMode="HighQuality"/>
                                        </Border>
                                        <Border Grid.Row="1" Grid.Column="1" Margin="2" CornerRadius="5" ClipToBounds="True" Background="Transparent">
                                            <Image Source="{Binding FolderPreviewIcon4}" Stretch="Uniform" RenderOptions.BitmapScalingMode="HighQuality"/>
                                        </Border>
                                    </Grid>
                                    <TextBlock Text="←"
                                               Visibility="{Binding BackNavigationVisibility}"
                                               FontSize="58"
                                               FontWeight="SemiBold"
                                               Foreground="{DynamicResource Brand}"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center"/>
                                    <TextBlock Text="↑"
                                               Visibility="{Binding ScrollTopNavigationVisibility}"
                                               FontSize="58"
                                               FontWeight="SemiBold"
                                               Foreground="{DynamicResource Brand}"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center"/>
                                </Grid>
                            </Border>
                            <Button Style="{StaticResource DocumentationBadgeStyle}"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Top"
                                Margin="6"
                                Click="DocumentationBadge_Click"/>
                            <Border>
                                <Border.Style>
                                    <Style TargetType="Border" BasedOn="{StaticResource FreshnessBadgeStyle}">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsNew}" Value="True">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <TextBlock Text="NEW" Foreground="{DynamicResource Brand}" FontWeight="SemiBold" FontSize="10"/>
                            </Border>
                            <Border>
                                <Border.Style>
                                    <Style TargetType="Border" BasedOn="{StaticResource FreshnessBadgeStyle}">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsUpdated}" Value="True">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <TextBlock Text="MAJ" Foreground="{DynamicResource Brand}" FontWeight="SemiBold" FontSize="10"/>
                            </Border>
                        </Grid>

                        <Grid Grid.Column="1" Margin="20,0,10,0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>

                            </Grid.RowDefinitions>

                            <DockPanel Grid.Row="0" LastChildFill="True">
                                <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                                    <Button Content="3D"
                                        Width="32" Height="22" FontSize="10" Padding="0"
                                        Margin="0,0,8,0"
                                        Click="OnPreview3DClick"
                                        ToolTip="Aperçu 3D de la famille"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        BorderBrush="Transparent"
                                        FocusVisualStyle="{x:Null}"
                                        Visibility="{Binding FamilyControlVisibility}"/>
                                    <Button Content="★"
                                        Click="FavoriteButton_Click"
                                        ToolTip="Ajouter/retirer des Favoris"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        FontSize="18"
                                        Width="32"
                                        HorizontalAlignment="Right"
                                        Margin="0,0,0,0"
                                        Visibility="{Binding FamilyControlVisibility}">
                                        <Button.Style>
                                            <Style TargetType="Button">
                                                <Setter Property="Foreground" Value="SkyBlue"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsFavorite}" Value="True">
                                                        <Setter Property="Foreground" Value="Orange"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                    </Button>
                                </StackPanel>
                                <TextBlock Text="{Binding Name}"
                                       Foreground="{DynamicResource PrimaryText}"
                                       FontWeight="Bold"
                                       FontSize="18"
                                       TextWrapping="Wrap"
                                       Margin="0,0,10,0"
                                       VerticalAlignment="Center"/>
                            </DockPanel>
                            <StackPanel Grid.Row="1"
                                        Margin="0,8,10,0"
                                        Visibility="{Binding SearchDetailsVisibility}">
                                <TextBlock Text="{Binding ApproximateMatchText}"
                                           Visibility="{Binding ApproximateMatchVisibility}"
                                           Foreground="#FFD97706"
                                           FontWeight="SemiBold"
                                           TextWrapping="Wrap"/>
                                <TextBlock Text="{Binding MatchedKeywordsText}"
                                           Visibility="{Binding MatchedKeywordsVisibility}"
                                           Foreground="#FF7C3AED"
                                           FontWeight="SemiBold"
                                           TextWrapping="Wrap"
                                           ToolTip="{Binding SearchDescription}"/>
                                <TextBlock Text="{Binding SearchLocationLabel}"
                                           Visibility="{Binding SearchLocationVisibility}"
                                           Foreground="{DynamicResource Brand}"
                                           FontWeight="SemiBold"
                                           FontSize="11"/>
                                <TextBlock Text="{Binding SearchFolderPath}"
                                           Visibility="{Binding SearchLocationVisibility}"
                                           Foreground="#FF6C7A92"
                                           TextWrapping="Wrap"
                                           ToolTip="{Binding SearchFolderPath}"/>
                            </StackPanel>
                            <WrapPanel Grid.Row="2" Margin="0,14,0,0">
                                <Button Click="CategoryPill_Click">
                                    <Button.Style>
                                        <Style TargetType="Button" BasedOn="{StaticResource PillButtonStyle}">
                                            <Setter Property="Background" Value="{DynamicResource Surface.Subtle}"/>
                                            <Setter Property="BorderBrush" Value="{DynamicResource Border}"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Category}" Value="">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Category}" Value="{x:Null}">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                    <TextBlock Text="{Binding Category}" Foreground="#FF1F3B57"/>
                                </Button>

                                <Button Click="VersionPill_Click">
                                    <Button.Style>
                                        <Style TargetType="Button" BasedOn="{StaticResource PillButtonStyle}">
                                            <Setter Property="Background" Value="#FFF2F5FC"/>
                                            <Setter Property="BorderBrush" Value="#FFDCE6FA"/>
                                            <Setter Property="Foreground" Value="#FF2A3F6A"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding RevitSavedVersion}" Value="">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding RevitSavedVersion}" Value="{x:Null}">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                    <TextBlock Foreground="#FF2A3F6A" FontWeight="SemiBold">
                                    <Run Text="Revit "/>
                                    <Run Text="{Binding RevitSavedVersion}"/>
                                    </TextBlock>
                                </Button>

                                <Button Click="SizePill_Click">
                                    <Button.Style>
                                        <Style TargetType="Button" BasedOn="{StaticResource PillButtonStyle}">
                                            <Setter Property="Background" Value="#FFFDF2E8"/>
                                            <Setter Property="BorderBrush" Value="#FFF6D9B8"/>
                                            <Setter Property="Foreground" Value="#FF594525"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding FileSizeText}" Value="">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding FileSizeText}" Value="{x:Null}">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=IsSizeSortActive}" Value="True">
                                                    <Setter Property="Background" Value="#FFF9E2C7"/>
                                                    <Setter Property="BorderBrush" Value="#FFE0A35C"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                    <TextBlock Text="{Binding FileSizeText}" Foreground="#FF594525" FontWeight="SemiBold"/>
                                </Button>
                            </WrapPanel>

                            <Border Grid.Row="3"
                                Margin="0,16,0,0"
                                Background="#FFFFFFFF"
                                BorderBrush="#22000000"
                                BorderThickness="1"
                                CornerRadius="10"
                                Padding="18">
                                <StackPanel>
                                    <TextBlock Text="Détails"
                                           Foreground="#FF1F3B57"
                                           FontWeight="SemiBold"
                                           FontSize="14"/>
                                    <Border Height="1"
                                        Margin="0,10,0,14"
                                        Background="#22000000"/>

                                    <Grid Margin="0,0,0,12">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="18"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0"
                                               Foreground="#FF6C7A92"
                                               FontWeight="SemiBold"
                                               Text="Catégorie"/>
                                        <TextBlock Grid.Column="2"
                                               Foreground="#FF1F3B57"
                                               Text="{Binding Category, TargetNullValue=—}"/>
                                    </Grid>

                                    <Grid Margin="0,0,0,12">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="18"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0"
                                               Foreground="#FF6C7A92"
                                               FontWeight="SemiBold"
                                               Text="Version Revit"/>
                                        <TextBlock Grid.Column="2"
                                               Foreground="#FF1F3B57"
                                               Text="{Binding RevitSavedVersion, TargetNullValue=—}"/>
                                    </Grid>

                                    <Grid Margin="0,0,0,12">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="18"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0"
                                               Foreground="#FF6C7A92"
                                               FontWeight="SemiBold"
                                               Text="Dernière mise à jour"/>
                                        <TextBlock Grid.Column="2"
                                               Foreground="#FF1F3B57"
                                               Text="{Binding LastUpdatedText, TargetNullValue=—}"/>
                                    </Grid>

                                    <Grid Margin="0,0,0,12">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="18"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0"
                                               Foreground="#FF6C7A92"
                                               FontWeight="SemiBold"
                                               Text="Taille du fichier"/>
                                        <TextBlock Grid.Column="2"
                                               Foreground="#FF1F3B57"
                                               Text="{Binding FileSizeText, TargetNullValue=—}"/>
                                    </Grid>

                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="18"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0"
                                               Foreground="#FF6C7A92"
                                               FontWeight="SemiBold"
                                               Text="Numéro OmniClass"/>
                                        <TextBlock Grid.Column="2"
                                               Foreground="#FF1F3B57"
                                              Text="{Binding OmniClassDisplay, TargetNullValue=—}"/>
                                    </Grid>
                                </StackPanel>
                            </Border>


                        </Grid>
                    </Grid>
                </Border>
            </DataTemplate>

            <DataTemplate x:Key="SearchGroupHeaderTemplate" DataType="{x:Type local:FamilyItem}">
                <Border Margin="6,14,6,6"
                        Width="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                        Background="{DynamicResource Surface.Subtle}"
                        BorderBrush="{DynamicResource Border}"
                        BorderThickness="0,1,0,1"
                        Padding="12,8">
                    <DockPanel LastChildFill="True">
                        <TextBlock DockPanel.Dock="Right"
                                   Text="{Binding SearchGroupCountText}"
                                   Foreground="{DynamicResource Text.Secondary}"
                                   FontWeight="SemiBold"
                                   Margin="18,0,0,0"/>
                        <TextBlock Text="{Binding SearchGroupTitle}"
                                   Foreground="{DynamicResource Text.Primary}"
                                   FontWeight="SemiBold"
                                   TextTrimming="CharacterEllipsis"
                                   ToolTip="{Binding SearchGroupTitle}"/>
                    </DockPanel>
                </Border>
            </DataTemplate>

            <DataTemplate x:Key="FamilySearchGroupTemplate" DataType="{x:Type local:FamilySearchGroup}">
                <StackPanel Margin="0,0,0,16">
                    <Border Margin="6,14,6,8"
                            Background="{DynamicResource Surface.Subtle}"
                            BorderBrush="{DynamicResource Border}"
                            BorderThickness="0,1,0,1"
                            Padding="12,8">
                        <DockPanel LastChildFill="True">
                            <TextBlock DockPanel.Dock="Right"
                                       Text="{Binding CountText}"
                                       Foreground="{DynamicResource Text.Secondary}"
                                       FontWeight="SemiBold"
                                       Margin="18,0,0,0"/>
                            <TextBlock Text="{Binding Title}"
                                       Foreground="{DynamicResource Text.Primary}"
                                       FontWeight="SemiBold"
                                       TextTrimming="CharacterEllipsis"
                                       ToolTip="{Binding Title}"/>
                        </DockPanel>
                    </Border>
                    <ItemsControl ItemsSource="{Binding Families}"
                                  ItemTemplate="{StaticResource FamilyItemTemplate}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel IsItemsHost="True"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                    </ItemsControl>
                </StackPanel>
            </DataTemplate>

            <local:FamilyItemTemplateSelector x:Key="FamilyListItemTemplateSelector"
                                              ItemTemplate="{StaticResource FamilyItemTemplate}"
                                              HeaderTemplate="{StaticResource SearchGroupHeaderTemplate}"/>

            <!-- Vignette famille (onglet Favoris/Collections) : bouton 🗑 -->
            <DataTemplate x:Key="CollectionItemTemplate" DataType="{x:Type local:FamilyItem}">
                <Border Margin="5"
                    Width="{Binding DataContext.TileCardWidth, RelativeSource={RelativeSource AncestorType=Window}}"
                    Background="{DynamicResource PanelBackground}"
                    CornerRadius="5"
                    MouseLeftButtonDown="FamilyItem_DoubleClick">
                    <Border.ContextMenu>
                        <ContextMenu>
                            <MenuItem Header="Rentrer dans la famille"
                                  Click="OpenFamilyFile_Click"
                                  DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                            <MenuItem Header="Charger la dernière version"
                                  Click="ReloadFamily_Click"
                                  DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                            <MenuItem Header="Mots-clés de recherche…"
                                  Click="SearchMetadataMenuItem_Click"
                                  DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                        </ContextMenu>
                    </Border.ContextMenu>

                    <Grid Margin="5">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <Grid Grid.Row="0">
                            <Border Style="{StaticResource PreviewFrameStyle}">
                                <Image Source="{Binding Icon}"
                                   Width="{Binding DataContext.TilePreviewSize, RelativeSource={RelativeSource AncestorType=Window}}"
                                   Height="{Binding DataContext.TilePreviewSize, RelativeSource={RelativeSource AncestorType=Window}}"
                                   Stretch="Uniform"
                                   RenderOptions.BitmapScalingMode="HighQuality"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   />
                            </Border>
                            <Button Style="{StaticResource DocumentationBadgeStyle}"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Top"
                                Margin="6"
                                Click="DocumentationBadge_Click"/>
                            <Border>
                                <Border.Style>
                                    <Style TargetType="Border" BasedOn="{StaticResource FreshnessBadgeStyle}">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsNew}" Value="True">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <TextBlock Text="NEW" Foreground="{DynamicResource Brand}" FontWeight="SemiBold" FontSize="10"/>
                            </Border>
                            <Border>
                                <Border.Style>
                                    <Style TargetType="Border" BasedOn="{StaticResource FreshnessBadgeStyle}">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsUpdated}" Value="True">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <TextBlock Text="MAJ" Foreground="{DynamicResource Brand}" FontWeight="SemiBold" FontSize="10"/>
                            </Border>
                        </Grid>

                        <Grid Grid.Row="1">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- 🗑 : supprimer de la collection courante (taille réduite) -->
                            <Button Grid.Column="0"
        Click="RemoveFromCollection_Click"
        ToolTip="Retirer de la collection courante"
        Background="Transparent"
        BorderThickness="0"
        Padding="0"
        Width="24" Height="24"
        HorizontalAlignment="Center"
        VerticalAlignment="Top"
        Margin="0,0,6,0"
        HorizontalContentAlignment="Center"
        VerticalContentAlignment="Center"
        Focusable="True">
                                <!-- Icône poubelle animée (hover) -->
                                <ui:AnimatedDeleteIcon IconSize="18"
                         Foreground="{DynamicResource PrimaryText}"/>
                            </Button>

                            <TextBlock Grid.Column="1"
           Text="{Binding Name}"
           Foreground="{DynamicResource PrimaryText}"
           FontWeight="Bold"
           FontSize="{Binding DataContext.TileNameFontSize, RelativeSource={RelativeSource AncestorType=Window}}"
           TextWrapping="Wrap"
           TextTrimming="CharacterEllipsis"
           LineStackingStrategy="BlockLineHeight"
           LineHeight="{Binding DataContext.TileNameLineHeight, RelativeSource={RelativeSource AncestorType=Window}}"
           MaxHeight="{Binding DataContext.TileNameMaxHeight, RelativeSource={RelativeSource AncestorType=Window}}"
           MaxWidth="{Binding DataContext.TileNameMaxWidth, RelativeSource={RelativeSource AncestorType=Window}}"
           TextAlignment="Center"
           HorizontalAlignment="Center"
           Margin="0,0,5,0"
           ToolTip="{Binding Name}"/>

                        </Grid>
                        <Button Content="3D"
        Width="28" Height="20" FontSize="10" Padding="0"
        HorizontalAlignment="Right" VerticalAlignment="Top" Margin="4"
        Click="OnPreview3DClick"
        ToolTip="Aperçu 3D de la famille"
        Background="Transparent"
        BorderThickness="0"
        BorderBrush="Transparent"
        FocusVisualStyle="{x:Null}"/>

                    </Grid>
                </Border>
            </DataTemplate>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Background="{DynamicResource BackgroundGradient}">
        <Grid Margin="8">
            <TabControl
                    Margin="0"
                    x:Name="MainTabControl"
                    Background="{DynamicResource TabBackground}"
                    Foreground="{DynamicResource PrimaryText}"
                    SelectionChanged="MainTabControl_SelectionChanged">
                <TabControl.Resources>
                    <Style TargetType="TabItem">
                        <Setter Property="Margin" Value="0,0,4,0"/>
                        <Setter Property="Padding" Value="12,6"/>
                        <Setter Property="MinWidth" Value="110"/>
                        <Setter Property="Foreground" Value="{DynamicResource TabInactiveForeground}"/>
                        <Setter Property="Background" Value="{DynamicResource Surface.Subtle}"/>
                        <Setter Property="BorderBrush" Value="{DynamicResource Border}"/>
                        <Setter Property="BorderThickness" Value="1"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="TabItem">
                                    <Border x:Name="TabBorder"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        Padding="{TemplateBinding Padding}"
                                        Margin="{TemplateBinding Margin}"
                                        CornerRadius="8,8,0,0">
                                        <ContentPresenter HorizontalAlignment="Center"
                                                      VerticalAlignment="Center"
                                                      ContentSource="Header"
                                                      RecognizesAccessKey="True"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="TabBorder" Property="Background" Value="{DynamicResource Brand}"/>
                                            <Setter TargetName="TabBorder" Property="BorderBrush" Value="{DynamicResource Brand.Hover}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="Background" Value="{DynamicResource Brand}"/>
                                <Setter Property="BorderBrush" Value="{DynamicResource Brand.Hover}"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </TabControl.Resources>

                <!-- Onglet Dossiers -->
                <TabItem Header="Dossiers" Background="{DynamicResource TabBackground}">
                    <Grid Margin="6" Background="Transparent">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="250"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0"
                            Background="{DynamicResource ContentPanelBackground}"
                            BorderBrush="{DynamicResource Border}"
                            BorderThickness="1"
                            CornerRadius="8"
                            Padding="8">
                            <StackPanel Background="Transparent">
                                <Button x:Name="AllFamiliesButton"
                                Content="{Binding RootFolderName}"
                                FontSize="14" FontWeight="Bold"
                                Foreground="{DynamicResource PrimaryText}"
                                Background="Transparent"
                                BorderThickness="0"
                                Height="35"
                                HorizontalContentAlignment="Left"
                                Click="AllFamiliesButton_Click"/>
                                <TreeView x:Name="FolderTreeView"
                                  Background="{DynamicResource TreeViewBackground}"
                                  Foreground="{DynamicResource PrimaryText}"
                                  Height="500"
                                  ScrollViewer.VerticalScrollBarVisibility="Auto"
                                  SelectedItemChanged="FolderTreeView_SelectedItemChanged"/>
                            </StackPanel>
                        </Border>

                        <Border Grid.Column="1"
                            Margin="8,0,0,0"
                            Background="{DynamicResource ContentPanelBackground}"
                            BorderBrush="{DynamicResource Border}"
                            BorderThickness="1"
                            CornerRadius="8"
                            Padding="8">
                            <Grid Background="Transparent">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <DockPanel Grid.Row="0" Margin="2,0,2,8" LastChildFill="True">
                                    <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Center">
                                        <Border Background="#FFF2F5FC"
                                                BorderBrush="#FFDCE6FA"
                                                BorderThickness="1"
                                                CornerRadius="10"
                                                Padding="8,3"
                                                Margin="8,0,0,0">
                                            <TextBlock x:Name="FolderSummaryText"
                                                       Foreground="#FF2A3F6A"
                                                       FontWeight="SemiBold"/>
                                        </Border>
                                    </StackPanel>
                                    <ItemsControl x:Name="BreadcrumbItemsControl"
                                                  ItemsSource="{Binding BreadcrumbItems}">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <WrapPanel IsItemsHost="True" VerticalAlignment="Center"/>
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                                    <TextBlock Text="›"
                                                               Margin="4,0"
                                                               Foreground="{DynamicResource Text.Secondary}"
                                                               Visibility="{Binding SeparatorVisibility}"/>
                                                    <Button Content="{Binding Name}"
                                                            Tag="{Binding Path}"
                                                            Style="{StaticResource BreadcrumbButtonStyle}"
                                                            Click="BreadcrumbButton_Click"/>
                                                </StackPanel>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </DockPanel>

                                <!-- Recherche -->
                                <Grid Grid.Row="1">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBox x:Name="SearchBox"
                                     Grid.Column="0"
                                     Height="30"
                                     VerticalAlignment="Center"
                                     TextChanged="SearchBox_TextChanged"
                                     KeyDown="SearchBox_KeyDown"
                                     GotKeyboardFocus="SearchBox_GotKeyboardFocus"
                                     LostKeyboardFocus="SearchBox_LostKeyboardFocus"
                                     Background="{DynamicResource SearchBoxBackground}"
                                     Foreground="{DynamicResource SearchBoxForeground}"
                                     BorderBrush="{DynamicResource SearchBoxBorder}"
                                     ToolTipService.InitialShowDelay="650"
                                     ToolTipService.ShowDuration="30000"
                                     Padding="8,4"
                                     Margin="2,0,8,0">
                                        <TextBox.ToolTip>
                                            <ToolTip>
                                                <StackPanel MaxWidth="360">
                                                    <TextBlock Text="Astuces de recherche"
                                                               FontWeight="SemiBold"
                                                               Margin="0,0,0,6"/>
                                                    <TextBlock TextWrapping="Wrap"
                                                               Text="En mode Tout, tu peux chercher dans toute la bibliothèque. Écris simplement un nom, ou ajoute un mot-clé pour préciser ce que tu veux trouver."/>
                                                    <TextBlock Text="Exemples utiles"
                                                               FontWeight="SemiBold"
                                                               Margin="0,10,0,4"/>
                                                    <TextBlock TextWrapping="Wrap" Text="new : familles ajoutées récemment" Margin="0,0,0,2"/>
                                                    <TextBlock TextWrapping="Wrap" Text="maj : familles modifiées récemment" Margin="0,0,0,2"/>
                                                    <TextBlock TextWrapping="Wrap" Text="rvt:2024 : familles enregistrées en Revit 2024" Margin="0,0,0,2"/>
                                                    <TextBlock TextWrapping="Wrap" Text="doc:oui : familles avec documentation" Margin="0,0,0,2"/>
                                                    <TextBlock TextWrapping="Wrap" Text="image:non : familles sans image d'aperçu" Margin="0,0,0,2"/>
                                                    <TextBlock TextWrapping="Wrap" Text="dossier:skid : chercher uniquement dans les dossiers contenant SKID"/>
                                                    <TextBlock TextWrapping="Wrap" Text="Une petite faute peut être corrigée automatiquement si aucun résultat exact n’existe." Margin="0,8,0,2"/>
                                                    <TextBlock TextWrapping="Wrap" Text="Après une recherche sans résultat, appuie sur Entrée pour mémoriser le terme. BIMaestro pourra ensuite proposer de l’associer à la famille finalement ouverte."/>
                                                    <TextBlock Text="Tu peux combiner : pompe rvt:2024 doc:oui"
                                                               Foreground="{DynamicResource Text.Secondary}"
                                                               Margin="0,10,0,0"
                                                               TextWrapping="Wrap"/>
                                                </StackPanel>
                                            </ToolTip>
                                        </TextBox.ToolTip>
                                    </TextBox>
                                    <TextBlock x:Name="PlaceholderText"
                                       Grid.Column="0"
                                       Text="Rechercher dans le dossier…"
                                       Foreground="Gray"
                                       Margin="10,0,0,0"
                                       VerticalAlignment="Center"
                                       IsHitTestVisible="False"/>
                                    <ToggleButton x:Name="SearchCurrentButton"
                                                  Grid.Column="1"
                                                  Content="Dossier"
                                                  IsChecked="True"
                                                  MinWidth="72"
                                                  Height="30"
                                                  Margin="0,0,4,0"
                                                  Click="SearchScopeButton_Click"
                                                  ToolTip="Rechercher uniquement dans le dossier ouvert"/>
                                    <ToggleButton x:Name="SearchAllButton"
                                                  Grid.Column="2"
                                                  Content="Tout"
                                                  MinWidth="58"
                                                  Height="30"
                                                  Click="SearchScopeButton_Click"
                                                  ToolTip="Rechercher dans toute la bibliothèque indexée. Exemples : new, maj, rvt:2024, dossier:skid, doc:oui."/>
                                </Grid>

                                <Rectangle Grid.Row="2" Height="1" Fill="{DynamicResource Divider}" Margin="0,5"/>

                                <ScrollViewer x:Name="ItemsScroll"
                                      Grid.Row="3"
                                      Background="{DynamicResource ItemsBackground}"
                                      VerticalScrollBarVisibility="Auto"
                                      ScrollChanged="ItemsScroll_ScrollChanged">
                                    <StackPanel>

                                        <!-- Top-8 -->
                                        <ItemsControl x:Name="TopFamiliesView"
                                              ItemTemplate="{StaticResource FamilyItemTemplate}"
                                              Visibility="Collapsed">
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <WrapPanel IsItemsHost="True"/>
                                                </ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                        </ItemsControl>

                                        <Rectangle x:Name="TopSeparator"
                                           Height="1"
                                           Fill="#CCCCCC"
                                           Margin="0,5"
                                           Visibility="Collapsed"/>

                                        <!-- Résultats paginés -->
                                        <ItemsControl x:Name="FamilyListView"
                                              ItemTemplateSelector="{StaticResource FamilyListItemTemplateSelector}"
                                              ItemsPanel="{StaticResource FamilyListWrapPanel}"/>

                                        <ItemsControl x:Name="GroupedFamilyListView"
                                                      ItemTemplate="{StaticResource FamilySearchGroupTemplate}"
                                                      Visibility="Collapsed"/>

                                        <TextBlock x:Name="PagingStatusText"
                                           Margin="8,12,8,20"
                                           HorizontalAlignment="Center"
                                           Foreground="{DynamicResource PrimaryText}"
                                           Visibility="Collapsed"/>
                                    </StackPanel>
                                </ScrollViewer>

                                <DockPanel Grid.Row="4" Margin="0,4,0,0">
                                    <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" VerticalAlignment="Center">
                                        <Button x:Name="DateSortButton"
        Content="Date de MAJ"
        Click="DateSortButton_Click">
                                            <Button.Style>
                                                <Style TargetType="Button" BasedOn="{StaticResource PillButtonStyle}">
                                                    <Setter Property="Margin" Value="0,0,12,0"/>
                                                    <Setter Property="Padding" Value="12,4"/>
                                                    <Setter Property="Background" Value="#FFF2F5FC"/>
                                                    <Setter Property="BorderBrush" Value="#FFDCE6FA"/>
                                                    <Setter Property="Foreground" Value="#FF2A3F6A"/>

                                                    <!-- ✅ Les triggers doivent être dans Style.Triggers -->
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=IsDateSortActive}" Value="True">
                                                            <Setter Property="Background" Value="#FFE0F2FF"/>
                                                            <Setter Property="BorderBrush" Value="#FF5AA0FF"/>
                                                        </DataTrigger>

                                                        <DataTrigger Binding="{Binding IsChecked, ElementName=DetailedViewCheckBox}" Value="False">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>
                                        </Button>

                                        <CheckBox x:Name="MultiSelectCheckBox"
                                          Content="Sélection multiple"
                                          Foreground="{DynamicResource PrimaryText}"
                                          Margin="0,0,20,0"
                                          Checked="MultiSelectCheckBox_Changed"
                                          Unchecked="MultiSelectCheckBox_Changed"/>
                                        <CheckBox x:Name="DetailedViewCheckBox"
                                          Content="Vue détaillée"
                                          Margin="0,0,8,0"
                                          Foreground="{DynamicResource PrimaryText}"
                                          Checked="DetailedViewCheckBox_Changed"
                                          Unchecked="DetailedViewCheckBox_Changed" Width="92"/>
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                                        <TextBlock Foreground="{DynamicResource PrimaryText}" Margin="5,0,0,0" Text="Éléments affichés : "/>
                                        <TextBlock x:Name="CountTextBlock" Foreground="{DynamicResource PrimaryText}" FontWeight="Bold"/>
                                    </StackPanel>
                                </DockPanel>
                            </Grid>
                        </Border>
                    </Grid>
                </TabItem>

                <!-- Onglet Favoris / Collections -->
                <TabItem Header="Favoris" Background="{DynamicResource TabBackground}">
                <Grid Margin="10" Background="Transparent">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Background="{DynamicResource ItemsBackground}">
                        <DockPanel LastChildFill="False">
                            <WrapPanel Orientation="Horizontal" DockPanel.Dock="Left" VerticalAlignment="Center">
                                <TextBlock Text="Collection :" VerticalAlignment="Center"
                                           Margin="0,0,8,0" Foreground="{DynamicResource PrimaryText}"/>
                                <ComboBox x:Name="CollectionCombo"
                                          Width="300"
                                          DisplayMemberPath="Name"
                                          Margin="6,0,0,0"
                                          SelectionChanged="CollectionCombo_SelectionChanged"/>
                                <Button Content="Nouv." Margin="6,0,0,0" Style="{StaticResource FavoritesActionButton}" Click="CollectionNew_Click" Background="{DynamicResource Surface.Subtle}"/>
                                <Button Content="Ren." Margin="6,0,0,0" Style="{StaticResource FavoritesActionButton}" Click="CollectionRename_Click" Background="{DynamicResource Surface.Subtle}"/>
                                <Button Content="Suppr." Margin="6,0,0,0" Style="{StaticResource FavoritesActionButton}" Click="CollectionDelete_Click" Background="{DynamicResource Surface.Subtle}"/>
                            </WrapPanel>

                            <WrapPanel Orientation="Horizontal" DockPanel.Dock="Right" VerticalAlignment="Center">
                                <Button x:Name="CollectionActionsButton"
                                        Content="Partager"
                                        Margin="0,0,10,0"
                                        Style="{StaticResource FavoritesActionButton}"
                                        Click="CollectionActionsButton_Click" Background="{DynamicResource Surface.Subtle}" IsDefault="True">
                                    <Button.ContextMenu>
                                        <ContextMenu>
                                            <MenuItem Header="Partager la collection…"
                                                      Click="CollectionShareMenuItem_Click"/>
                                            <MenuItem Header="Télécharger la collection…"
                                                      Click="CollectionDownloadMenuItem_Click"/>
                                        </ContextMenu>
                                    </Button.ContextMenu>
                                </Button>
                                <Button Content="Charger la collection"
                                        Style="{StaticResource FavoritesPrimaryActionButton}"
                                        Click="CollectionLoad_Click"/>
                            </WrapPanel>
                        </DockPanel>
                    </Border>

                    <ScrollViewer Grid.Row="1" Background="{DynamicResource ItemsBackground}">
                        <ItemsControl x:Name="CollectionListView"
                                      ItemTemplate="{StaticResource CollectionItemTemplate}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel IsItemsHost="True"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                        </ItemsControl>
                    </ScrollViewer>

                    <StackPanel Orientation="Horizontal" Grid.Row="2" HorizontalAlignment="Right" Margin="0,6,0,0">
                        <TextBlock Text="Éléments dans la collection : " Foreground="{DynamicResource PrimaryText}"/>
                        <TextBlock x:Name="CollectionCountText" Foreground="{DynamicResource PrimaryText}" FontWeight="Bold"/>
                    </StackPanel>
                </Grid>
            </TabItem>

            <!-- Onglet Paramètres -->
            <TabItem x:Name="SettingsTabItem"
                     Header="Paramètres"
                     Background="{DynamicResource TabBackground}">
                <ScrollViewer VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled"
                              Padding="0"
                              Focusable="False">
                    <Grid x:Name="SettingsPanelRoot" Margin="12,10,8,10" Background="Transparent">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Grid.Resources>
                        <Style x:Key="SettingLabelStyle" TargetType="TextBlock">
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="Foreground" Value="{DynamicResource PrimaryText}"/>
                            <Setter Property="FontSize" Value="12"/>
                            <Setter Property="Margin" Value="0,0,16,0"/>
                        </Style>
                        <Style x:Key="SettingSectionTitleStyle" TargetType="TextBlock">
                            <Setter Property="Foreground" Value="{DynamicResource PrimaryText}"/>
                            <Setter Property="FontSize" Value="14"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="Margin" Value="0,0,0,10"/>
                        </Style>
                        <Style x:Key="SettingSectionBorderStyle" TargetType="Border">
                            <Setter Property="Background" Value="#19FFFFFF"/>
                            <Setter Property="BorderBrush" Value="#22000000"/>
                            <Setter Property="BorderThickness" Value="1"/>
                            <Setter Property="CornerRadius" Value="6"/>
                            <Setter Property="Padding" Value="14"/>
                            <Setter Property="Margin" Value="0,0,0,12"/>
                        </Style>
                    </Grid.Resources>

                    <TextBlock Grid.Row="0"
                               Text="Paramètres d'apparence"
                               Style="{StaticResource SettingSectionTitleStyle}"/>

                    <Border Grid.Row="1"
                            Style="{StaticResource SettingSectionBorderStyle}">
                        <Grid Grid.IsSharedSizeScope="True">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" SharedSizeGroup="SettingLabel"/>
                                <ColumnDefinition Width="180"/>
                                <ColumnDefinition Width="32"/>
                                <ColumnDefinition Width="Auto" SharedSizeGroup="SettingLabel"/>
                                <ColumnDefinition Width="180"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Row="0" Grid.Column="0" Text="Couleur du haut" Style="{StaticResource SettingLabelStyle}"/>
                            <ui:SimpleColorPicker Grid.Row="0" Grid.Column="1"
                                              x:Name="TopColorPicker"
                                              Height="26"
                                              SelectedColor="#FFF2F2F2"/>

                            <TextBlock Grid.Row="0" Grid.Column="3" Text="Couleur du bas" Style="{StaticResource SettingLabelStyle}"/>
                            <ui:SimpleColorPicker Grid.Row="0" Grid.Column="4"
                                              x:Name="BottomColorPicker"
                                              Height="26"
                                              SelectedColor="#FFFFFFFF"/>

                            <TextBlock Grid.Row="1" Grid.Column="0" Text="Arrière-plan des familles" Style="{StaticResource SettingLabelStyle}" Margin="0,12,16,0"/>
                            <ui:SimpleColorPicker Grid.Row="1" Grid.Column="1"
                                              Margin="0,12,0,0"
                                              x:Name="PanelBackgroundPicker"
                                              Height="26"
                                              SelectedColor="#F0F0F0"/>

                            <TextBlock Grid.Row="1" Grid.Column="3" Text="Arrière-plan de l'arborescence" Style="{StaticResource SettingLabelStyle}" Margin="0,12,16,0"/>
                            <ui:SimpleColorPicker Grid.Row="1" Grid.Column="4"
                                              Margin="0,12,0,0"
                                              x:Name="TreeViewBackgroundPicker"
                                              Height="26"
                                              SelectedColor="#F0F0F0"/>

                            <TextBlock Grid.Row="2" Grid.Column="0" Text="Arrière-plan des panneaux" Style="{StaticResource SettingLabelStyle}" Margin="0,12,16,0"/>
                            <ui:SimpleColorPicker Grid.Row="2" Grid.Column="1"
                                              Margin="0,12,0,0"
                                              x:Name="ContentPanelBackgroundPicker"
                                              Height="26"
                                              SelectedColor="#F0F0F0"/>

                            <TextBlock Grid.Row="2" Grid.Column="3" Text="Taille des vignettes" Style="{StaticResource SettingLabelStyle}" Margin="0,12,16,0"/>
                            <ComboBox Grid.Row="2" Grid.Column="4"
                                      Margin="0,12,0,0"
                                      x:Name="TileSizeCombo"
                                      Height="26"
                                      SelectedValuePath="Tag"
                                      SelectionChanged="TileSizeCombo_SelectionChanged">
                                <ComboBoxItem Content="Compact" Tag="Compact"/>
                                <ComboBoxItem Content="Normal" Tag="Normal" IsSelected="True"/>
                                <ComboBoxItem Content="Large" Tag="Large"/>
                            </ComboBox>
                        </Grid>
                    </Border>

                    <TextBlock Grid.Row="2"
                               Text="Couleurs secondaires"
                               Style="{StaticResource SettingSectionTitleStyle}"/>

                    <Border Grid.Row="3"
                            Style="{StaticResource SettingSectionBorderStyle}">
                        <Grid Grid.IsSharedSizeScope="True">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" SharedSizeGroup="SettingLabel"/>
                                <ColumnDefinition Width="180"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Row="0" Grid.Column="0" Text="ItemsBackground" Style="{StaticResource SettingLabelStyle}"/>
                            <ui:SimpleColorPicker Grid.Row="0" Grid.Column="1"
                                              x:Name="ItemsBackgroundPicker"
                                              Height="26"
                                              SelectedColor="Transparent"/>

                            <TextBlock Grid.Row="1" Grid.Column="0" Text="Arrière-plan général" Style="{StaticResource SettingLabelStyle}" Margin="0,12,16,0"/>
                            <ui:SimpleColorPicker Grid.Row="1" Grid.Column="1"
                                              Margin="0,12,0,0"
                                              x:Name="TabBackgroundPicker"
                                              Height="26"
                                              SelectedColor="Transparent"/>
                        </Grid>
                    </Border>

                    <TextBlock Grid.Row="4"
                               Text="Comportement"
                               Style="{StaticResource SettingSectionTitleStyle}"/>

                    <Border Grid.Row="5"
                            Style="{StaticResource SettingSectionBorderStyle}">
                        <Grid Grid.IsSharedSizeScope="True">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" SharedSizeGroup="SettingLabel"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Row="0" Grid.Column="0" Text="Mode sombre" Style="{StaticResource SettingLabelStyle}"/>
                            <ui:EyeToggleSwitch Grid.Row="0" Grid.Column="1"
                                                x:Name="DarkModeSwitch"
                                                Width="200"
                                                BaseSize="18"
                                                TransitionMs="420"
                                                VerticalAlignment="Center"
                                                Toggled="DarkModeSwitch_Toggled"/>

                            <TextBlock Grid.Row="1" Grid.Column="0" Text="Afficher le Top-8" Style="{StaticResource SettingLabelStyle}" Margin="0,12,16,0"/>
                            <CheckBox Grid.Row="1" Grid.Column="1"
                                      Margin="0,12,0,0"
                                      x:Name="ShowTop8CheckBox"
                                      VerticalAlignment="Center"
                                      IsChecked="False"
                                      Checked="ShowTop8CheckBox_Changed"
                                      Unchecked="ShowTop8CheckBox_Changed"/>

                            <TextBlock Grid.Row="2" Grid.Column="0" Text="Toujours au-dessus" Style="{StaticResource SettingLabelStyle}" Margin="0,12,16,0"/>
                            <ui:GhostSwitch Grid.Row="2" Grid.Column="1"
                                            Margin="0,12,0,0"
                                            x:Name="AlwaysOnTopSwitch"
                                            Width="160"
                                            Height="46"
                                            EyeFollowGlobal="False"
                                            AutoPauseWhenHidden="True"
                                            Toggled="AlwaysOnTopSwitch_Toggled"/>
                        </Grid>
                    </Border>

                    <Grid Grid.Row="6" Margin="0,8,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Bouton à gauche -->
                        <Button Content="Modifier les chemins…" 
            Width="220" 
            HorizontalAlignment="Left"
            Margin="0,0,16,0"
            Click="ChangePaths_Click"/>

                        <!-- Trois boutons à droite -->
                        <StackPanel Orientation="Horizontal"
                Grid.Column="1"
                HorizontalAlignment="Right">

                            <Button Content="Appliquer" 
                Width="120" 
                Margin="0,0,8,0" 
                Click="ApplyColors_Click"/>

                            <Button Content="Enregistrer" 
                Width="120" 
                Margin="0,0,8,0" 
                Click="SaveConfig_Click"/>

                            <Button Content="Réinitialiser" 
                Width="120" 
                Click="ResetConfig_Click"/>
                        </StackPanel>
                    </Grid>

                    <TextBlock Grid.Row="7"
                               Text="Export d'aperçus 3D"
                               Style="{StaticResource SettingSectionTitleStyle}"/>

                    <Border Grid.Row="8"
                            Style="{StaticResource SettingSectionBorderStyle}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Row="0" Grid.ColumnSpan="3"
                                       Text="Sélectionne le dossier de familles à traiter puis le dossier miroir pour les PNG exportés."
                                       Foreground="{DynamicResource PrimaryText}"
                                       Margin="0,0,0,12"/>

                            <TextBlock Grid.Row="1" Grid.Column="0"
                                       Text="Dossier familles"
                                       Style="{StaticResource SettingLabelStyle}"/>
                            <TextBox Grid.Row="1" Grid.Column="1"
                                     x:Name="PreviewSourceTextBox"
                                     IsReadOnly="True"
                                     Margin="0,0,8,6"/>
                            <Button Grid.Row="1" Grid.Column="2"
                                    Content="Choisir…"
                                    Width="100"
                                    Click="SelectPreviewFolders_Click"/>

                            <TextBlock Grid.Row="2" Grid.Column="0"
                                       Text="Dossier miroir"
                                       Style="{StaticResource SettingLabelStyle}"/>
                            <TextBox Grid.Row="2" Grid.Column="1"
                                     x:Name="PreviewTargetTextBox"
                                     IsReadOnly="True"
                                     Margin="0,0,8,6"/>
                            <Button Grid.Row="2" Grid.Column="2"
                                    Content="Choisir…"
                                    Width="100"
                                    Click="SelectPreviewFolders_Click"/>

                            <StackPanel Grid.Row="3" Grid.ColumnSpan="3" Orientation="Horizontal" Margin="0,8,0,6">
                                <Button x:Name="StartPreviewButton"
                                        Content="Lancer l'export 3D"
                                        Width="170"
                                        Click="StartPreviewExport_Click"
                                        Margin="0,0,12,0"/>
                                <Button x:Name="CancelPreviewButton"
                                        Content="Annuler"
                                        Width="120"
                                        IsEnabled="False"
                                        Click="CancelPreviewExport_Click"/>
                                <TextBlock x:Name="PreviewProgressText"
                                           Foreground="{DynamicResource PrimaryText}"
                                           VerticalAlignment="Center"
                                           Margin="16,0,0,0"/>
                            </StackPanel>

                            <StackPanel Grid.Row="4" Grid.ColumnSpan="3">
                                <ProgressBar x:Name="PreviewProgressBar"
                                             Height="16"
                                             Minimum="0"
                                             Maximum="1"
                                             Visibility="Collapsed"/>
                                <TextBox x:Name="PreviewLogTextBox"
                                         Margin="0,10,0,0"
                                         Height="140"
                                         TextWrapping="Wrap"
                                         IsReadOnly="True"
                                         VerticalScrollBarVisibility="Auto"/>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <TextBlock Grid.Row="9"
                               Text="Mots-clés intelligents"
                               Style="{StaticResource SettingSectionTitleStyle}"/>

                    <Border Grid.Row="10"
                            Style="{StaticResource SettingSectionBorderStyle}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Margin="0,0,18,0">
                                <TextBlock Text="Enrichir toute la bibliothèque"
                                           FontWeight="SemiBold"
                                           Foreground="{DynamicResource PrimaryText}"/>
                                <TextBlock Margin="0,5,0,0"
                                           Foreground="{DynamicResource Text.Secondary}"
                                           TextWrapping="Wrap"
                                           Text="Affiche l’aperçu, le nom, la description et les mots-clés proposés pour chaque famille. Vous pouvez tout corriger avant l’enregistrement."/>
                            </StackPanel>
                            <Button Grid.Column="1"
                                    Content="Ouvrir l’assistant…"
                                    Width="180" Height="34"
                                    VerticalAlignment="Center"
                                    Click="OpenSearchMetadataReview_Click"/>
                        </Grid>
                    </Border>
                    </Grid>
                </ScrollViewer>
            </TabItem>

        </TabControl>
            <Button x:Name="HelpButton"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Top"
                    Width="32"
                    Height="24"
                    Margin="0,6,8,0"
                    Content="?"
                    FontWeight="Bold"
                    ToolTip="Ouvrir l'aide en ligne"
                    Style="{StaticResource SecondaryButton}"
                    Click="HelpButton_Click"/>
        </Grid>
    </Grid>
</Window>
